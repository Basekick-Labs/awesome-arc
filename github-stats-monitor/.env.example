# Arc Configuration (REQUIRED)
# Arc API URL
ARC_URL=http://localhost:8000

# Arc API token (REQUIRED - get this from your Arc instance)
ARC_TOKEN=your-arc-api-token-here

# Arc database to write to (optional, default: "default")
ARC_DATABASE=default

# GitHub Configuration (REQUIRED)
# Comma-separated list of repositories to monitor (format: owner/repo)
GITHUB_REPOS=basekick-labs/arc,basekick-labs/arc-superset-dialect

# GitHub personal access token (optional but recommended for higher rate limits)
# Without this, you're limited to 60 requests/hour. With token: 5000 requests/hour
# Create token at: https://github.com/settings/tokens
GITHUB_TOKEN=

# Monitoring Configuration
# Interval between collection cycles in seconds (default: 600 = 10 minutes)
INTERVAL_SECONDS=600
